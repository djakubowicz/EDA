---
name: Test Rulebook
hosts: localhost
sources:
  - ansible.eda.range:
      limit: 5
rules:
  - name: Always true
    condition: event.i > 0
    action:
      run_playbook:
        name: generic-handle.yml
